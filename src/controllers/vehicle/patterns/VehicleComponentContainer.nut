/**
 * VehicleComponentContainer describe Entity Component System logic for vehicles.
 * In that container could be stored number of different VehicleComponents by its type.
 * But also that container could store only one record with unique subtype of VehicleComponent.
 */
class VehicleComponentContainer extends Container {
    
    

}
